

describe('writing specs in typescript', () => {
    it('is easy', () => {
        expect(true).toBe(true);
    });

    it('can add two numbers'), () => {
        const a = 10;
        const b = 20;

        const answer = a + b;

        expect(answer).toBe(30);
    }
});


describe('name of desc', () => {

    it('this is a spec', () => {

    });

});